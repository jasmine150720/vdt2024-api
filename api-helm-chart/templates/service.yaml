apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-helm-chart.fullname" . }}
  labels:
    {{- include "api-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-helm-chart.selectorLabels" . | nindent 4 }}
